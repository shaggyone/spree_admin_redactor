= image_tag @imperavi_asset.file.url(@imperavi_asset.file_style)

= form_tag admin_imperavi_asset_destroy_url(:id => @imperavi_asset, :return => :list) do
  = submit_tag t('destroy'), :class => 'destroy'

